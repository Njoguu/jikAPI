Get all availeble jobs posted 
---
tags:
  - Postings

description: Returns all jobs from the system that the user has access to 
summary: List of postings
responses:
  200:
    description: OK.
    content:
      application/json:
        schema:
          type: array