Get all available jobs posted 
---
tags:
  - Postings

description: Returns all jobs that have been posted
summary: List of postings
parameters:
  - name: page
    in: query
    description: Page number for the results
    required: false
    type: integer
  - name: per_page
    in: query
    description: Number of results per page
    required: false
    type: integer
responses:
  200:
    description: OK
    content:
      application/json:
        schema:
          type: array
  204:
    description: No jobs found