Returns refreshed access token
---
tags:
  - User

description: Returns a newly refreshed access_token of the logged in user -> Use the refresh token given on Login as Bearer token
responses:
  200:
    description: OK
    content:
      application/json:
        schema:
          type: object
  401: 
    description: Unauthorized

security:
  - Bearer: ['Authorization']  